<%= form_for(@category) do |f| %>
  
  <div class="row">
    <div class="form-group col-md-8">
      <%= f.text_field :name, placeholder: "Nombre de la categoría", required: true, class: "form-control" %>
    </div>
  </div>
  
  <div class="row">
    <div class="form-group col-md-12">
      <%= f.text_area :include_words, placeholder: "Palabras que incluye separadas por espacios en blanco", 
              required: true, class: "form-control" %>
    </div>
  </div>
  
  <div class="row">
    <div class="form-group col-md-12">
      <%= f.text_area :exclude_words, placeholder: "Palabras que excluye separadas por espacios en blanco", 
              class: "form-control" %>
    </div>
  </div>
  
  <div class="actions">
    <%= f.submit "Guardar", class: 'btn btn-success' %>			
  </div>
<% end %>
